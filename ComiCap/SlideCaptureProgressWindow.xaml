<Window x:Class="ComiCap.SlideCaptureProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="スライドキャプチャ実行中"
        Width="450"
        Height="200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        Topmost="True">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ステータスラベル -->
        <TextBlock x:Name="StatusLabel"
                   Grid.Row="0"
                   Text="準備中..."
                   FontSize="14"
                   FontWeight="Bold"
                   Margin="0,0,0,15"
                   TextAlignment="Center"/>

        <!-- 進捗バー -->
        <ProgressBar x:Name="ProgressBar"
                     Grid.Row="1"
                     Height="25"
                     Margin="0,0,0,15"
                     Minimum="0"
                     Maximum="100"
                     Value="0"/>

        <!-- 進捗詳細 -->
        <TextBlock x:Name="DetailLabel"
                   Grid.Row="2"
                   Text=""
                   FontSize="12"
                   Foreground="Gray"
                   TextAlignment="Center"
                   VerticalAlignment="Center"/>

        <!-- 停止ボタン -->
        <Button x:Name="StopButton"
                Grid.Row="3"
                Content="停止"
                Width="100"
                Height="30"
                HorizontalAlignment="Center"
                Click="StopButton_Click"/>
    </Grid>
</Window>
